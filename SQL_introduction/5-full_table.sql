-- Cette commande affiche la requête SQL complète utilisée pour créer la table "first_table".
-- Elle permet de visualiser la structure exacte de la table, y compris les types de données, les contraintes, 
-- et les options de création, comme si on la recréait à partir de zéro.
SHOW CREATE TABLE first_table;