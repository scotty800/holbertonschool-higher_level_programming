-- Cette commande calcule la moyenne des scores dans la table "second_table".
-- La fonction AVG(score) retourne la moyenne des valeurs de la colonne "score".
-- Le résultat est affiché avec l'alias "average" pour clarifier la sortie.
SELECT AVG(score) AS average FROM second_table;