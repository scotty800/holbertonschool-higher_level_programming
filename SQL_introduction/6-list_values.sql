-- Cette commande sélectionne et affiche toutes les colonnes et toutes les lignes de la table "first_table".
-- Elle est utile pour visualiser l'ensemble des données actuellement stockées dans la table.
SELECT * FROM first_table;